
{{#if container}}
// {{name}} Container Specs
{{else}}
// {{name}} Component Specs
{{/if}}

// import { expect } from 'chai';
// import spies from 'chai-spies';
// chai.use(spies)

// Testing Utils
// import shallow from '../../../utils/componentTest';
// import defaultInjections from '../../../utils/defaultInjections';

// Prepare The Component
// const injector = require('!!vue?inject!../index.vue')
// let Component = injector({
  // ...defaultInjections()
// })

/*
{{#if container}}
describe('containers/{{name}}', function () {
{{else}}
describe('components/{{name}}', function () {
{{/if}}

  let vm;
  beforeEach(function () {
    vm = shallow(Component)
  })

  it('Some Test', function () {
    const actual = true
    expect(actual).to.be.equal(true)
  })

})
*/
